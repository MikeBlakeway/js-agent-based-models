const CANVAS = {
    WIDTH: window.innerWidth * 0.98,
    HEIGHT: window.innerHeight * 0.95,
}

const AGENT = {
    MAX_POPULATION: 2,
}

const PHYSICS = {
    C: 10,
}

export { CANVAS, AGENT, PHYSICS }
